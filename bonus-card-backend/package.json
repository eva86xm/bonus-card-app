{
  "name": "bonus-card-backend",
  "version": "1.0.0",
  "description": "Backend API for the bonus card app prototype.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/services/snc.service.js && node --check src/controllers/snc.controller.js && node --check src/middleware/rate-limit.middleware.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "axios": "^1.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18"
  }
}
